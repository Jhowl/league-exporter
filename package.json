{
  "name": "league-exporter",
  "version": "0.1.0",
  "private": true,
  "description": "Dockerized Dota 2 league match exporter powered by OpenDota Explorer SQL.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
